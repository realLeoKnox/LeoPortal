version: '3.8'

services:
  leo-portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leo-portal
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
